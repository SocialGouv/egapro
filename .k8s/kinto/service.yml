---
kind: Service
apiVersion: v1
metadata:
  name: kinto
  labels:
    app: ${STARTUP}
    component: kinto
    namespace: ${STARTUP}-${ENVIRONMENT_NAME}
spec:
  selector:
    component: kinto
  ports:
  - port: ${PORT}
    name: kinto
