---
kind: Service
apiVersion: v1
metadata:
  name: kinto
  labels:
    app: egapro
    component: kinto
    namespace: egapro-${ENVIRONMENT_NAME}
spec:
  selector:
    component: kinto
  ports:
  - port: ${PORT}
    name: kinto
