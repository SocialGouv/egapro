---
kind: Service
apiVersion: v1
metadata:
  name: egapro-api${SUFFIX_BRANCH_NAME}
  labels:
    app: egapro-api${SUFFIX_BRANCH_NAME}
    branch: egapro${SUFFIX_BRANCH_NAME}
spec:
  selector:
    app: egapro-api${SUFFIX_BRANCH_NAME}
  ports:
  - port: ${PORT}
    name: api
