---
kind: Service
apiVersion: v1
metadata:
  name: api
  labels:
    app: ${STARTUP}
    component: api
    namespace: ${STARTUP}-${NAMESPACE_ENVIRONMENT}
spec:
  selector:
    component: api
  ports:
    - port: ${PORT}
      name: api
