---
kind: Service
apiVersion: v1
metadata:
  name: api
  labels:
    app: egapro
    component: api
    namespace: egapro-${ENVIRONMENT_NAME}
    job: "${CI_JOB_ID}"
spec:
  selector:
    component: api
  ports:
    - port: ${PORT}
      name: api
