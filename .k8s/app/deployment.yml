---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: egapro-app${SUFFIX_BRANCH_NAME}
  labels:
    app: egapro
    environment: ${ENVIRONMENT_NAME}
    branch: ${ENVIRONMENT_BRANCH}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: egapro-app${SUFFIX_BRANCH_NAME}
  template:
    metadata:
      labels:
        app: egapro-app${SUFFIX_BRANCH_NAME}
    spec:
      containers:
        - image: ${EGAPRO_REGISTRY}/app:${IMAGE_TAG}
          name: egapro-app
          ports:
            - containerPort: ${PORT}
          livenessProbe:
            httpGet:
              path: /
              port: ${PORT}
            initialDelaySeconds: 3
            periodSeconds: 5
          readinessProbe:
            httpGet:
              path: /
              port: ${PORT}
            initialDelaySeconds: 3
            periodSeconds: 5
          resources:
            requests:
              cpu: 5m
              memory: 16Mi
            limits:
              cpu: 50m
              memory: 32Mi
          env:
            - name: REACT_APP_API_URL
              value: "http://egapro-api${SUFFIX_BRANCH_NAME}:4000"
