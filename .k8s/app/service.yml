---
kind: Service
apiVersion: v1
metadata:
  name: app
  labels:
    app: egapro
    component: app
    namespace: egapro-${ENVIRONMENT_NAME}
spec:
  selector:
    component: app
  ports:
    - port: ${PORT}
      name: app
