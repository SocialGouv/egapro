---
kind: Service
apiVersion: v1
metadata:
  name: app
  labels:
    app: ${STARTUP}
    component: app
    namespace: ${STARTUP}-${NAMESPACE_ENVIRONMENT}
spec:
  selector:
    component: app
  ports:
    - port: ${PORT}
      name: app
