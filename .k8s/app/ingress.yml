---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app
  labels:
    app: egapro
    component: app
    namespace: egapro-${ENVIRONMENT_NAME}
  annotations:
    kubernetes.io/ingress.class: "azure/application-gateway"
    appgw.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
  - host: ${ENVIRONMENT_NAME}.egapro.${APP_DOMAIN_NAME}
    http:
      paths:
        - path: /
          backend:
            serviceName: app
            servicePort: ${PORT}
  tls:
    - secretName: crt
