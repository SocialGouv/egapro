---
apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: app
  labels:
    app: egapro
    component: app
    namespace: egapro-${ENVIRONMENT_NAME}
    job: "${CI_JOB_ID}"
  annotations:
    kubernetes.io/ingress.class: "azure/application-gateway"
    appgw.ingress.kubernetes.io/ssl-redirect: "true"
spec:
  rules:
  - host: ${ENVIRONMENT_NAME}.egapro.${ENVIRONMENT}.social.gouv.fr
    http:
      paths:
        - path: /
          backend:
            serviceName: app
            servicePort: ${PORT}
  tls:
    - secretName: crt
